# Escon Soluções Contábeis — Contexto canônico (memória compartilhada)

> **Leia este arquivo no início de qualquer trabalho** sobre a Escon, agentes contábeis, Radar, Secretaria, Oneflow ou Contmatic.  
> Atualizado por humanos e por agentes (Grok / Claude / Codex).  
> Caminho do projeto de agentes: `C:\Users\ander\OneDrive\Desktop\Projetos\Agentes Contabeis Escon`

---

## Escritório

| Campo | Valor |
|-------|--------|
| Nome | Escon Soluções Contábeis |
| Marca digital | Escon Digital (`escondigital.com.br`) |
| Responsável | Anderson |
| Carteira | ~**85 Simples Nacional** + ~**15 MEI** (somente esses 2 regimes) |
| Prioridade atual #1 | **Lançamentos contábeis no Contmatic** (zerar atraso e migrar 100% Contábil para Oneflow) |
| Custo legado | ~**R$ 1.100/mês** no sistema antigo até concluir contabilidades atrasadas |

---

## Sistemas em uso

| Sistema | Papel | Status |
|---------|--------|--------|
| **Oneflow** | DP + Fiscal (produção); Contábil em **fase de cadastro** | Destino final 100% |
| **Sistema legado** (antigo principal ~5 anos) | Contabilidades atrasadas ainda rodam aqui | Manter até migrar; custo R$ 1.100/mês |
| **Contmatic** | Importação de lançamentos contábeis (Excel do pipeline) | Prioridade operacional #1 |
| **Planilhas** | Controle de clientes e do escritório | Em uso paralelo |
| **Radar Escon** | Consultas RFB / SEFAZ / documentos → Google Drive; empresas cadastradas | `C:\Users\ander\OneDrive\Desktop\Projetos\Radar Escon` |
| **Secretaria** (fazer.ai agents + Chatwoot/EsconZap) | Agente WhatsApp em produção | `C:\Users\ander\OneDrive\Desktop\Projetos\Secretaria` |
| **EsconZap / CRM WhatsApp** | Canal de mensagens | Já integrado à Secretaria |
| **escon-contabil** | App contábil próprio (Next + .NET) | `Projetos\escon-contabil` / produção `contabil.escondigital.com.br` |
| **Agentes Contábeis Escon** | Multiagente Contmatic + dashboard operacional | este repositório |
| **Contabilizador** (skill) | XML/OFX/PDF → Excel Contmatic | `~\.claude\skills\user\contabilizador\` |

### O que NÃO duplicar

- **WhatsApp / atendimento**: já coberto pela **Secretaria**. Bella neste repo gera rascunhos/offline; produção = Secretaria.
- **Busca RFB/SEFAZ/Drive**: **Radar Escon**. Agentes leem artefatos baixados; não reimplementar portais sem necessidade.
- **DP e Fiscal do dia a dia**: **Oneflow** (já 100%).

---

## Prioridades (ordem)

1. **Lançamentos Contmatic** — processar pasta do cliente → Excel importável → revisão humana → importar  
2. Zerar atraso no legado e **migrar Contábil para Oneflow** (eliminar R$ 1.100/mês)  
3. Dashboard operacional para colaboradoras (fluxos, status, solicitar serviços)  
4. Integração Radar → Agentes → Contmatic (**import-radar + sync-inbox já implementados**)  
5. Manter Secretaria como fronteira WhatsApp (não competir)

---

## LLM / modelos

- Provedor preferido: **OpenRouter** (`OPENROUTER_API_KEY`)
- Modelos desejados (aliases no sistema):

| Alias | Uso típico | ID OpenRouter (ajustar se mudar) |
|-------|------------|-----------------------------------|
| `kimi` | Raciocínio / longo contexto | `moonshotai/kimi-k2` |
| `gpt` / `gps` | Geral (usuário escreve GPS = GPT) | `openai/gpt-4o-mini` |
| `grok` | Geral / xAI via OpenRouter | `x-ai/grok-4` |
| `deepseek` | Custo/benefício código e texto | `deepseek/deepseek-chat` |
| `gemini` | Multimodal / Google | `google/gemini-2.5-flash` |
| `claude` | Análise cuidadosa | `anthropic/claude-sonnet-4` |

Também aceita `XAI_API_KEY` direto se quiser xAI sem OpenRouter.

---

## Agentes deste repositório (Contábil)

| ID | Foco |
|----|------|
| max | Orquestrador + status |
| xavier | XMLs fiscais |
| bill | PDFs/recibos |
| john | Conciliação OFX |
| greg | Cobrança de extratos (mensagem; envio real = Secretaria) |
| anne | Tarefas/prazos |
| cesar | Certidões (cadastro local) |
| fernando | Certificados digitais A1 (lê Radar `credenciais_serpro`, avisa 15 dias antes / vencidos) |
| pedro | Cadastro de empresas (Sistema Acessórias = fonte de verdade → cadastro local / Radar) |
| paul | Insights financeiros |
| lucy / karen | Reforma / notícias |
| bella / rachel | Rascunhos (WhatsApp produção = Secretaria) |

Pipeline Contmatic (prioridade): **Xavier → Bill → (John opcional) → Excel Contmatic → revisão humana**.

---

## Pastas e dados

```
Agentes Contabeis Escon/
  data/inbox/{cliente}/   # XML, OFX, PDF do período
  data/outbox/{cliente}/  # Excel Contmatic, índices, conciliações
  data/tasks/             # runs dos agentes + solicitações do dashboard
  data/clients/           # cadastro local (espelho; fonte rica = Radar)
  docs/ESCON-CONTEXTO.md  # ESTE arquivo (fonte de verdade narrativa)
```

Dashboard: `python -m escon_agentes dashboard` → http://127.0.0.1:8787

---

## Preferências do Anderson / Escon

- Foco em **resultado operacional** ( Contmatic, prazos, custo do legado ), não em demos sem uso.
- **Humano no loop** em lançamentos e orientações fiscais.
- Não reinventar o que Secretaria e Radar já fazem bem.
- Multi-modelo via OpenRouter; poder trocar Kimi / GPT / Grok / DeepSeek.
- Documentar decisões neste arquivo quando mudarem sistemas ou prioridades.
- Carteira só **Simples + MEI**.

---

## Como agentes (Grok/Claude/Codex) devem usar esta memória

1. Ler **este arquivo** no início de tarefas Escon.  
2. Se existir skill `escon-context`, seguir o SKILL.md.  
3. Complementar com `memory_search` / claude-mem se disponível.  
4. Ao concluir mudança estrutural, **atualizar este arquivo** (não só o chat).  
5. Projetos relacionados: listar paths absolutos Windows do Anderson (OneDrive Desktop Projetos).

---

## Changelog de contexto

| Data | Nota |
|------|------|
| 2026-08-01 | Criação: Oneflow DP/Fiscal, legado R$1100, Contmatic P1, Secretaria WhatsApp, Radar 100 empresas, OpenRouter multi-modelo, dashboard colaboradoras |
| 2026-08-01 | **Import Radar:** 87 empresas (todas `simples` no banco) → `data/clients/` + pastas `data/inbox/{cnpj}`. Comandos: `import-radar`, `sync-inbox`. Fonte VPS `76.13.224.42` / Postgres `radar-escon-db-1`. Docs MinIO espelhados no Drive (27k+). MEI ainda não aparece separado no Radar (só tag simples). |
| 2026-08-01 | **Drive→inbox:** `sync-drive --via drive|minio|auto` (+ `--watch`). Drive Desktop ainda não montado nesta máquina; fallback MinIO = mesma árvore do Drive. |
| 2026-08-01 | **Plano Contmatic unificado:** `config/plano_contas.yaml` + motor `contabilizador_engine.py` (skill Contabilizador). Códigos reais (1121101, 4111201, 1112201…). Pipeline Contmatic usa o motor completo. |
| 2026-08-01 | **Escon_Lancamento absorvido:** modelos em `data/models/` (`PlContas.TXT`, `0001_2026_lctos.xlsx`, PDF). Layout Excel 10 colunas + data `DD.MM.AAAA`. Projeto antigo em `Projetos\Escon_Lancamento` pode ser **excluído** — tudo necessário já está nos Agentes. |
| 2026-08-01 | **Dashboard Vercel:** `index.html` + `/api` (login, chat multiagente OpenRouter, agentes, clientes). Deploy: `docs/DEPLOY-VERCEL.md`. GitHub: `Andersonprocesso/escon-agentes-contabeis`. Ops pesadas (Contmatic/sync) no Python local. |
| 2026-08-01 | **Handoff Obsidian:** `Obsidian Vault/Escon/Handoff - Agentes Contabeis 2026-08-01.md` + `docs/HANDOFF.md` no repo. Usar para retomar com outra IA quando créditos acabarem. |
| 2026-08-01 | **Redesign dashboard Vercel:** tema claro/escuro persistido, navbar/sidebar refeitas, avatares (cliente + equipe), perfil de cliente no painel lateral ao clicar na linha (resumo, contato, ações), ícones SVG próprios. Corrigidos 2 bugs reais: drawer mobile não escondia (ordem de CSS/media query) e tabela de clientes estourava a largura da tela (`.main` sem `min-width:0`). Só `index.html` mudou; `dashboard/static/` e `api/*.js` intocados. |
| 2026-08-01 | **Rachel lê e-mail real:** `contato@escondigital.com.br` é Microsoft 365 (MX Outlook); IMAP bloqueado por Conditional Access/MFA moderno, sem app password disponível. Solução: app registrado no Azure AD (`Escon Raquel Email`, permissões delegadas Mail.Read/Mail.ReadWrite) + login único via device code (MSAL) + Microsoft Graph API (`src/escon_agentes/tools/graph_mail.py`). Comando `raquel-emails`: cruza remetente com `data/clients/*.json` (campo `email` — hoje só 1 de 87 preenchido), cria rascunho de resposta real na caixa (nunca envia), separa anexos por cliente/ano/mês em `data/outbox/email_attachments/` (upload pro Drive ainda manual), e marca/relata e-mails de não-clientes. Token cache em `.msal_cache/` (gitignored). |
| 2026-08-01 | **Agente Fernando Batista — certificados digitais:** lê `credenciais_serpro` do Postgres do Radar via SSH (mesmo padrão de `radar_sync.py`, só metadados — nunca o blob/senha do certificado), cruza com `radar_id` do cadastro local, avisa vencidos + a vencer em 15 dias, gera oferta de renovação (rascunho de mensagem) e tarefa no board — nunca envia direto (Secretaria/WhatsApp faz o envio real). Comando `python -m escon_agentes certificados`. Descoberta na 1ª rodada: 4 certificados já vencidos (Apolo, L C Reinaldo, Ana Gabriela, Rocha e Reinaldo). Dedupe por `radar_id+valido_ate` em `data/outbox/certificados_avisos_state.json`. |
| 2026-08-02 | **Agente Pedro Henrique — cadastro via Acessórias:** API `api.acessorias.com` (Bearer token, 100 req/min, `GET /companies/ListAll` paginado 20/pág, `POST /companies` cria+atualiza, **sem DELETE**). Sync é código puro (zero token): `cadastro-sync` compara em bloco e salva snapshot em `data/acessorias_snapshot.json` (reprocessa com `--cache`). **Campos reais ≠ documentação**: vem `Identificador`/`Razao`/`Regime` como texto (não inteiro 0-10). **113 empresas** no Acessórias vs 87 no local → aplicado: 117 clientes, **112 com e-mail** (era 1!), **19 MEI separados** (resolve gap antigo do Radar). 2 armadilhas tratadas: (a) `anjubiel.anju@gmail.com` está em 111/113 empresas (contato do escritório p/ receber envios) — filtrado via `OFFICE_CONTACT_EMAILS`, senão todo cliente ficaria com o e-mail do Anderson e quebraria a Rachel; (b) `Telefone` do Acessórias é compartilhado (30 números p/ 113 empresas) vs individual no Radar (62) — virou `FILL_ONLY_FIELDS`, nunca sobrescreve, senão quebraria cobrança do Greg no WhatsApp. Política: criar=livre, alterar=confirmação (`--aplicar --confirmar-alteracoes`), excluir=nunca. Radar ainda não recebe escrita (só diff). Falta: ler CNPJ/documentos anexados p/ cadastrar (parte com LLM). |
| 2026-08-02 | **Pedro: cadastro a partir de documentos** (`cadastro-novo <arquivo|pasta>`): lê cartão CNPJ/contrato social e extrai os campos. Regex/rótulo resolve tudo em doc padrão da RFB (13 campos, **0 chamada de LLM**); o modelo só é acionado uma vez, para os campos que sobraram, num único JSON. Sem `--criar` é simulação. **2 bugs achados e corrigidos em teste:** (a) `_unwrap_list` não reconhecia `Identificador`/`Razao` (chaves reais do GET unitário), então a checagem de duplicidade sempre dava 'não existe' e um POST passou num CNPJ existente — sem dano (dados idênticos, verificado: 113 empresas, 0 duplicatas); (b) falha na checagem era *fail-open* → agora é **fail-closed**: sem conseguir confirmar duplicidade, não cadastra. PDF escaneado (imagem) não é suportado — exigiria OCR. |
| 2026-08-02 | **Rachel × Pedro (efeito em cadeia) + convenção do Drive:** `raquel-recheck` reavalia e-mails marcados como 'não-cliente' depois que o cadastro muda — necessário porque a classificação depende do cadastro (e-mail lido com cadastro vazio vira não-cliente por engano). Achou o caso real: nota fiscal da Nascimento de Araujo (klinger@klctur.com.br) estava parada como pendência genérica; reprocessada → rascunho criado, estrela removida, 2 PDFs baixados. **Convenção real do Drive descoberta** (`Radar/backend/app/services/storage.py`): `{Empresa}/{Departamento}/{Ano}/{MM-AAAA}/` — tem nível de **departamento** (Impostos, Notas Fiscais-Entrada/Saida/Servico, Extratos, eSocial, e-CAC, Recibos, Comprovantes, Situação Fiscal) e mês é `07-2026`, não `07-Julho`. Corrigido. Novo `departamento_drive.py` classifica por **conteúdo, nunca pelo nome**: o arquivo 'NotaFiscal … - JUREMA ÁGUA QUENTE.pdf' parecia entrada, mas o emitente é o próprio cliente → é `Notas Fiscais-Servico` (saída). Também filtra `image001.png` (assinatura de e-mail). **Pendente:** upload ao Drive — ver nota seguinte sobre reusar o pipeline do Radar. |
| 2026-08-02 | **Ponte Rachel → Radar → Drive** (`anexos-para-drive`): em vez de subir ao Drive por fora, o anexo é entregue ao pipeline do Radar chamando a **própria função dele** (`salvar_documento` dentro do container `radar-escon-api-1` via SSH) — herda sha256, storage_key determinístico, upsert idempotente e o worker `drive.espelhar_todos` (de hora em hora, min 20) leva ao Drive com lock anti-duplicata. Custo zero de token (roda no servidor). Departamento vira `tipo` do Radar (Notas Fiscais-Servico→`nfse_xml` etc.); nada no Radar faz parse por tipo, então PDF com tipo `*_xml` é seguro — verificado. Testado E2E: 2 NFS-e da Nascimento de Araujo → MinIO → Drive (`drive_file_id` preenchido, 32.047 bytes íntegros). Não classificado = fica parado para humano, nunca chuta pasta. Aprendizados de infra: binário vai por **stdin do ssh** (embutir no comando estoura o limite ~32 KB do Windows) e o script precisa de `sys.path.insert(0,'/code')`. Também: `cli.py` força stdout UTF-8 (cp1252 derrubava comandos ao imprimir '→'/'⏸'). **Lição:** não criar pastas no Drive na mão — o Radar cria a árvore dele e as minhas viraram duplicatas vazias. |
| 2026-08-03 | **Pedro fecha o lado Radar** (`cadastro-radar`): `build_radar_plan` era código morto (nunca chamado) — agora tem comando com diff. Cria empresas faltantes reusando o schema `EmpresaCreate` do Radar (herda validação de dígito verificador do CNPJ e defaults de `config_radar`/`monitoramento_ativo`); **alterações nunca são aplicadas**, só reportadas. **Bug corrigido:** o Radar usa vocabulário curto de regime (`simples`) e o cadastro local o longo (`simples_nacional`) — a comparação textual marcava **83 de 87 empresas como divergentes** e proporia sobrescrever o Radar à toa; com `regime_para_radar()` caiu para 4 divergências reais. Aplicado: **30 empresas criadas no Radar** → `import-radar` → **117 clientes, todos com radar_id (era 30 sem)**. Isso destrava o `anexos-para-drive`, que exige radar_id. Divergências reais que sobraram (não tocadas): 2 MEIs que o Radar tem como `simples`, 1 UF (SP vs MG), 1 razão social. Infra: payload grande vai por arquivo/stdin — embutir no script estoura o limite de linha de comando do Windows. |
| 2026-08-03 | **Correções pós-uso (Anderson conferiu):** (1) **Pedro importava empresa baixada** — `normalize_company` já extraía `ativa` (Status do Acessórias), mas os planos ignoravam; 8 inativas entraram no cadastro e no Radar (ex.: Margarete Vilma, baixada em 06/10/2025). Corrigido: `build_local_plan`/`build_radar_plan` pulam `Status != Ativa` e reportam em `inativas`. Cadastro local: 117 → **109**. (2) **Dashboard mostrava 87** — o snapshot `data/web/clients_snapshot.json` nunca foi reexportado após o sync; agora 109. `scripts/export_clients_snapshot.py` também ganhou o fix de UTF-8. (3) **Pedro e Fernando faltavam no dashboard Vercel** (`api/agents-data.js` + persona em `api/chat.js` + equipe no prompt do Max). (4) **Rachel não é automática** — só roda por comando; não há agendamento. Ela cria rascunho **apenas para cliente cadastrado**; remetente desconhecido é marcado/reportado. (5) **Abas Destaques/Outros do Outlook não são problema** — o Graph lê a Inbox inteira (verificado: `inferenceClassification` 11 focused / 0 other). O que existe são ~40 pastas com regras movendo e-mail para fora da Inbox (Importante 1572, Certificados 1053, IRPF 617…) — Rachel hoje só lê a Caixa de Entrada. |
| 2026-08-03 | **Raquel automática + regras treináveis:** `config/regras_email.yaml` (editável pelo Anderson, comparação literal — sem LLM, sem token). Ações: `lixeira` (move para Itens Excluídos, **reversível** — nunca exclusão definitiva), `arquivar: Pasta`, `ignorar`. **Cliente cadastrado nunca é tocado por regra** — sempre rascunho + anexo. `--reaplicar-regras` reavalia e-mails já vistos quando uma regra nova é criada (não refaz rascunho de cliente, evitando duplicata). Agendamento: `scripts/raquel.bat` (log em `data/logs/raquel.log`); a tarefa do Windows deve ser criada pelo próprio Anderson. `get_access_token` na triagem passou a usar `interactive_ok=False` — rodando pelo agendador não há quem digite o código, então falha rápido em vez de travar. Testado: 3 e-mails tratados por regra, confirmados na Lixeira e fora da Caixa de Entrada. |
| 2026-08-03 | **Raquel agendada — 3 armadilhas do Agendador do Windows** (custaram várias tentativas, ficam registradas): (1) `schtasks /tr` com aspas simples no PowerShell **quebra o caminho no espaço** ('...\Agentes' virou o executável e 'Contabeis Escon\...' virou argumento) — usar `New-ScheduledTaskAction`/`Register-ScheduledTask`, que não sofre com aspas; (2) `.bat` criado por heredoc do bash sai com **LF misturado e acentos UTF-8**, que o `cmd.exe` não digere — reescrito em ASCII puro com CRLF e **caminhos absolutos** (inclusive o do `python.exe`), sem depender de PATH nem de diretório de trabalho; (3) **`DisallowStartIfOnBatteries` é `True` por padrão** — no notebook na bateria o Windows pula a tarefa e ainda reporta `LastTaskResult = 0`, dando falsa impressão de sucesso. Corrigido com `-AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -StartWhenAvailable`. Confirmado funcionando: tarefa executa de hora em hora e grava em `data/logs/raquel.log`. |
| 2026-08-03 | **Raquel estava na CAIXA ERRADA (erro grave) + 2 problemas de dados no cadastro:** (1) o login por device code foi feito como `anderson@escondigital.com.br` e o código usa `/me` — então rascunhos, marcações e movimentações aconteceram na **caixa pessoal**, não em `contato@`. Causa: ao tomar 403 em `/users/{mailbox}` eu troquei para `/me` sem validar identidade. Corrigido com `conferir_caixa()`, que **aborta** se o UPN do token ≠ `MS_GRAPH_MAILBOX` (divergência é falha, nunca aviso). Para trocar: apagar `.msal_cache/` e logar como `contato@`. (2) **3 clientes cadastrados com e-mail do próprio escritório** (inclusive a Escon como cliente dela mesma, e Margarida com `contato@`) — por isso notificações internas de quarentena (`Junk:`) viraram rascunho. (3) **11 e-mails pertencem a mais de uma empresa** (ex.: `anapaula.rangel1@outlook.com` em 5) — arquivar no palpite mandaria nota fiscal para a pasta da empresa errada. Ambos tratados em `indice_por_email()`: e-mail do domínio do escritório nunca identifica cliente, e e-mail ambíguo vira pendência explícita para o humano dizer de qual empresa é. Efeito: 74 e-mails identificam cliente com segurança, 11 bloqueados. |
| 2026-08-03 | **Agente Alexandre (lançamentos) + razão destrinchado:** `razao_parser.py` lê o Razão Analítico em PDF por **posição de coluna** (só o texto não diz se o valor é débito ou crédito) e extraiu **2.090 lançamentos reais** (2020+2022, 68 contas nomeadas). Deles saiu `config/regras_lancamento.yaml` (25 regras): DAS, folha, FGTS, pró-labore, combustível, energia, NFS-e etc. já vêm com débito/crédito/histórico prontos — **zero token no caso do dia a dia**. LLM só no que nenhuma regra reconhece, 1 chamada por documento, e a resposta é **descartada se citar conta fora do plano**. Data e valor nunca vêm do modelo. **Divisão de trabalho definida:** Xavier lê XML, John lê OFX e concilia (não lança), Bill lê PDF — só o Alexandre contabiliza, consumindo o que eles estruturam (sem duplicar parser). **ACHADO GRAVE:** `config/plano_contas.yaml` tem contas erradas — manda Simples Nacional para *IPI a recolher* (2131101) e pró-labore para *FGTS a recolher* (2141202); os códigos corretos são 2131115 e 2141102. As despesas (4121301/4121101/4121401/4121501) sequer existem; as reais são 3221101/3212102/3221107/4121205. `PlContas.TXT` só tem Ativo/Passivo — nenhuma conta de resultado. Razões copiados para `data/models/`. Plano de execução: Obsidian `Escon/Plano - Agentes Lancamentos Contabeis`. |
| 2026-08-03 | **Plano de contas corrigido — e correção de um erro meu:** eu havia concluído que `PlContas.TXT` estava truncado (só Ativo/Passivo). **Estava errado**: o arquivo sempre teve as contas de resultado; o `plcontas_parser.py` é que as descartava em silêncio, porque contas 3xxx/4xxx trazem uma **letra colada na reduzida** (`3111201C`, `4111101A`) e o regex exigia espaço. Corrigido: o índice passou de 266 → **477 contas**, com **cobertura total** das 68 usadas no razão. Com o plano completo, a auditoria do `config/plano_contas.yaml` confirmou o problema grave e maior do que eu havia dito: **22 de 37 contas erradas** — `simples_nacional` caía em *IPI a recolher*, `desp_salarios` em *IPI*, `prolabore_pagar` em *FGTS a recolher*, e 17 simplesmente não existiam. Todas corrigidas contra o uso real do razão; hoje **0 contas inexistentes**. `PlContas.TXT` substituído pela exportação nova (477 contas). Token da API Contmatic guardado no `.env`: `api.contmatic.com.br` responde, mas os endpoints do plano de contas ainda não foram descobertos (404 nos palpites) — falta a documentação. |
| 2026-08-03 | **API Contmatic Phoenix conectada** (`contmatic_api.py`): base `https://api.contmatic.com.br/public`, auth `Authorization: Bearer <token>` (token puro dá 401, apesar da doc dizer `apiKey`). Autentica como ESCON SOLUCOES CONTABEIS e lista **124 empresas** com `apelido` (Escon = `0001`, Nascimento de Araujo = `0088`) — o apelido é a chave dos demais serviços. **Liberado:** `/v1/clientes/self`, `/v1/empresas`, `/v1/usuarios`, `/v1/metadatas`, `/v1/cargos`, `/v1/horarios`. **Bloqueado:** `/v1/planocontas/{apelido}/{ano}` e `/v1/lancamentos/{apelido}/{ano}` — o Contmatic responde 422 *"O sistema ACESSORIA não pode usar este serviço"*: o token é do produto Acessórias, e esses dois pertencem ao Contábil. Para o Alexandre puxar o plano por empresa (hoje é um arquivo fixo, igual para todos) e enviar lançamento direto, é preciso um token emitido para o **Contábil**. `montar_lancamento()` já monta o corpo do POST, mas não envia — gravação em produção exige aprovação humana. |
| 2026-08-03 | **Fechamento por competência no painel local** (`workflows/fechamento.py` + tela no `dashboard/static/`): para zerar contabilidade atrasada. Escolhe cliente + competência (aceita `jan/21`, `01/2021`, `2021-01`), aponta **pasta do PC**, **pasta do OneDrive** e/ou **Drive via Radar**; roda o Alexandre e mostra o andamento etapa por etapa; no fim dá para **conferir e baixar a planilha** do Contmatic. Cada competência tem pasta própria (`data/inbox/{cliente}/{AAAA-MM}/`) para jan/21 não se misturar com fev/21, e o Excel sai nomeado por competência. Endpoints: `POST /api/pastas/inspecionar` (confere antes de rodar), `POST/GET /api/fechamentos`, `GET /api/fechamentos/{cliente}/{comp}/planilha`. **Precisa ser o painel local** — o do Vercel não lê pasta do PC. Testado E2E: 2 documentos → 3 lançamentos por regra, 0 LLM, planilha de 5.835 bytes baixada. Achado: com `from __future__ import annotations`, modelo Pydantic declarado **dentro** de `create_app()` faz o FastAPI tratar o corpo como query param (422) — precisa ficar no nível do módulo. **OneDrive:** código pronto, mas exige adicionar `Files.Read.All` ao app do Azure (o login atual só tem Mail.Read/ReadWrite) e refazer o login. |
| 2026-08-03 | **Dois logins Microsoft separados** (decisão do Anderson, para não repetir o episódio da caixa errada): perfil `mail` = `contato@escondigital.com.br` (Mail.Read/ReadWrite, cache `rachel_token_cache.json`) e perfil `arquivos` = `anderson@escondigital.com.br` (Files.Read.All, cache `arquivos_token_cache.json`). São contas diferentes e um token só não servia às duas. `get_access_token(perfil=...)` escolhe o cache e os escopos; `conferir_conta(perfil)` aborta se o login não for da conta esperada. Permissões concedidas no app do Azure (Files.Read, Files.Read.All, Files.ReadWrite + Mail). **OneDrive do anderson@ conectado e listando**. Pendente: o login do perfil `mail` como `contato@` — as duas tentativas caíram em anderson@ porque o navegador reaproveita a sessão; usar janela anônima. Esclarecimento: **Google Drive (Radar) e OneDrive são fontes distintas** no painel de fechamento — o Google Drive já funcionava pelo espelho MinIO e não dependia de nada disso. |
| 2026-08-03 | **Os dois logins Microsoft concluídos e validados.** Perfil `mail` = `contato@escondigital.com.br`, perfil `arquivos` = `anderson@escondigital.com.br` (OneDrive listando). O login da caixa só funcionou em **janela anônima** — o navegador reaproveitava a sessão do anderson@ e as duas tentativas anteriores foram recusadas pela trava. Primeira leitura da caixa correta mostra o material real de lançamento chegando por e-mail: XML da ALUMAX (Prime Center), comprovantes de DAS e de pagamentos (Salles Brazil), notas fiscais + extrato bancário — nada disso aparecia na caixa pessoal. |
| 2026-08-03 | **Alexandre corrigido em dados reais (Alumax jan/2021): 54 → 307 lançamentos, pendentes 259 → 6, tudo por regra e 0 token.** Três defeitos reais achados e corrigidos: (1) o Alexandre lia `data`/`valor` do XML, mas o Xavier devolve **`data_emissao`/`valor_total`** — toda nota fiscal caía em pendente por 'faltou data, valor'; (2) `xml_fiscal` não reconhecia **CFe (Cupom Fiscal Eletrônico / SAT)** — 200 dos 253 XMLs da Alumax são cupom de balcão e viravam 'desconhecido'; agora `detect_tipo` identifica CFe e lê `dEmi` (AAAAMMDD) + `vCFe`, com `_normalizar_data` unificando em AAAA-MM-DD; (3) **valores 100x maiores**: `_para_float` tratava todo ponto como separador de milhar, e o XML fiscal usa **ponto decimal** — R$ 20,70 virava R$ 2.070,00. Agora só converte no padrão brasileiro quando há vírgula. Conferência automática contra o XML de origem: **0 divergências**. Novas regras `venda_mercadoria` (cliente é emitente → D 1121101 / C 4111103) e `compra_mercadoria` (cliente é destinatário → D 5131102 / C 2121101); o sentido vem de comparar o CNPJ do emitente com o do cliente. |
| 2026-08-03 | **Alexandre: forma de pagamento, parcelamento e duplicatas.** Quem pede a competência informa `forma_pagamento` (**caixa** ou **banco**) e a conta `@recebimento` resolve para 1111101 ou para o banco do cliente. **Parcelamento**: `_tem_parcelamento` lê o bloco `<cobr><dup>` / `indPag=1` da NF-e — venda em 3x vira `venda_a_prazo` (D 1121101 Duplicatas), não caixa. **Cupom fiscal** (CFe/SAT) é sempre venda à vista → `venda_balcao`. **Duplicatas em PDF**: emitida (D 1121101 / C 4111103), recebida (D @recebimento / C 1121101) e **paga em atraso** — nesta os juros (3231105) e a multa (3221307) ficam num lançamento separado, sem se misturar ao principal. A ordem das regras importa (vence a primeira que casa): a de atraso vem antes da comum, senão os encargos se perdiam — bug encontrado no teste. Validado na Alumax jan/21 via caixa: 307 lançamentos, 200 `venda_balcao`, todas as vendas debitando 1111101. |
| 2026-08-03 | **Leitor de folha iniciado** (`tools/folha_parser.py`). Decisão de arquitetura: a folha ganha leitor próprio, mas **como leitor** (igual Xavier/John/Bill) — quem escreve a planilha continua sendo só o Alexandre. Motivo: uma NF-e vira 1 lançamento, uma folha vira um **conjunto que precisa fechar** (proventos + vantagens − descontos = líquido). O parser **confere antes de contabilizar**: folha que não fecha não gera lançamento, vai para revisão (`problemas()`). Validado na folha real de pró-labore da Alumax jan/21: 1 funcionário, proventos 1.250,00, INSS 137,50, líquido 1.112,50, `fecha=True`. Três defeitos achados e corrigidos no próprio teste: (1) linha de cabeçalho 'Cargo/Salário' contada como provento, dobrando o valor; (2) duas rubricas na mesma linha — só a primeira era lida, o INSS se perdia; (3) a chave `"13"` (13º salário) casava dentro de qualquer número (137,50 virava 13º). **Pendente:** testar numa folha com muitas rubricas (o Anderson sugeriu a da empresa Jorge Paulo Rodrigues Araujo Acabamento) — os PDFs dela têm nome numérico e a busca do Graph não os encontrou; precisa do caminho exato. Falta também mapear provisões (férias/13º) e criar o agente que consome este parser. |
| 2026-08-03 | **Folha validada em caso real e completo** (Jorge Paulo Rodrigues Araujo, ago/2020, link do SharePoint): **27 funcionários, todos fechando** — proventos 35.284,20, descontos 9.259,30, líquido 26.024,90. O teste com folha grande revelou o que a de pró-labore escondia: **o provento principal da folha da Escon é 'Horas Normais Diurnas'**, não 'Salário' — sem essa chave o parser lia só os descontos e perdia o salário do mês. Acrescentadas também horas extras, DSR, e os descontos de Taxa Assistencial / contribuição sindical. Agora as rubricas **somam exatamente** os totais declarados (1.886,97 / 936,93 no funcionário conferido). Para abrir link compartilhado do SharePoint: `u!` + base64url da URL em `/shares/{token}/driveItem`. Pendente: agente que consome o parser, provisões de férias/13º, e conferir se 'Taxa Assistencial' deve ir para conta própria (hoje cai em salarios_pagar). |
| 2026-08-03 | **Agente Fabiana (folha) criado e validado.** Ciclo completo: **provisão** no último dia da competência e **pagamento** nas datas do calendário da Escon — salário no **5º dia útil** do mês seguinte, adiantamento dia **20** do próprio mês, FGTS dia **7**, INSS dia **20** (se cair em fim de semana, vai para o próximo dia útil). **Cada holerite é individual**, com nome do empregado e competência no complemento; as guias (GPS/GRF) vão em lançamento único. Folha que não fecha **não gera lançamento nenhum**. Validado no Jorge ago/2020: 27 funcionários, 116 lançamentos (82 provisão + 34 pagamento), GPS 2.856,70 e GRF 2.667,43. **Bug grave achado no teste:** o tipo era do arquivo, não do funcionário — como o PDF é 'Folha de Pagamento **e** Pró-labore', os 27 empregados iam para conta de pró-labore (3212102). Agora o tipo é por funcionário. Também: 'Taxa Assistencial' virava adiantamento; foi para conta própria (`sindicato_pagar`, **código 2141103 precisa ser conferido**). **Pendente (pedido do Anderson):** pagamento em data avulsa e com juros/multa; provisão e pagamento de **rescisão** e **férias**. |
| 2026-08-03 | **Fabiana: provisões de férias e 13º.** Cálculo mensal que **não está na folha** — férias = 1/12 + 1/3 constitucional, 13º = 1/12, FGTS de 8% sobre as duas. Contas do próprio razão: D 3221105 / C 2141301 (férias), D 3221104 / C 2141302 (13º), FGTS em 2141305 e 2141306. **Sócio fica de fora** (pró-labore não tem férias nem 13º) e **não se provisiona INSS patronal**: a carteira da Escon é Simples Nacional, onde já está no DAS. Validado no Jorge ago/2020: 220 lançamentos (186 provisão + 34 pagamento); conferido num funcionário de base 1.886,97 → férias 209,66, 13º 157,25, FGTS 16,77 e 12,58. Pode ser desligado com `provisionar: False`. **Ainda pendente:** pagamento em data avulsa com juros/multa e **rescisão** (o Jorge tem pasta 'Relatorio Termo Rescisão' para servir de teste). |
| 2026-08-03 | **Anexo IV e rescisão na Fabiana.** (1) **Correção do Anderson:** empresas do **Anexo IV** (construção civil, limpeza, vigilância — caso da Jorge) recolhem a **CPP patronal fora do DAS**, via GPS; nos demais anexos ela já está no DAS. Virou configuração por cliente: `anexo_simples` e `aliquota_rat` no `ClientProfile`; quando anexo=4 a Fabiana provisiona CPP 20% + RAT como despesa da empresa e soma na mesma GPS. (2) **Rescisão**: verbas com conta própria (saldo de salário, aviso prévio indenizado, férias e 13º proporcionais, multa 40% do FGTS), conta a pagar separada (`rescisao_pagar` 2141106) e **sem constituir provisão** — na rescisão elas são baixadas, não criadas. Testado num TRCT: as verbas somam exatamente os proventos (6.113,34). **Dois bugs achados no teste:** rubrica genérica capturava a específica ('Saldo de Salário' virava salário comum e o valor era contado duas vezes) — resolvido com prioridade da verba específica e valor contado uma vez só; e o detector de rescisão casava com qualquer 'rescis' no texto, fazendo a folha comum do Jorge inteira virar rescisão (os 27 foram para 'Rescisão a pagar') — agora exige marcador forte (TERMO DE RESCISÃO DO CONTRATO, TRCT). **Códigos a conferir:** 2141103 sindicato, 3221108 CPP, 3221109 aviso prévio, 3221110 multa FGTS. |
| 2026-08-03 | **Fabiana: pagamento em data avulsa com juros e multa.** `input['pagamentos']` aceita a data real do comprovante por item (`salario`, `adiantamento`, `fgts`, `inss`) e, quando houver atraso, `juros`/`multa`. Sem isso vale o calendário padrão. **O principal nunca muda** — sai pelo valor provisionado, para a conta a pagar zerar; juros (3231105) e multa (3221307) entram em **lançamento separado**, em contas do próprio razão. Testado no Jorge: FGTS pago em 15/09 → principal 2.667,43 + juros 12,50 + multa 33,34, e INSS remarcado para 25/09. |
| 2026-08-03 | **TRCT real testado — precisa de parser próprio.** O termo de rescisão oficial (`TermoRescisao_Func_42.pdf`, Jorge) usa o **formulário do governo com campos numerados**, não o layout 'Func:' do sistema de folha: `50 Saldo de Salário`, `63 13º Proporcional 3/12`, `65 Férias Proporcionais 4/12`, `68 Terço Constitucional`, `69 Aviso Prévio Indenizado`, `TOTAL BRUTO`. A Fabiana **detecta** que é rescisão mas lê 0 funcionários — e reporta isso em vez de gerar lançamento errado. As regras contábeis de rescisão (contas por verba, conta a pagar própria, não constituir provisão) já estão prontas e validadas num TRCT montado à mão; falta só o leitor do formulário oficial, que é estruturado e padronizado. **Não usar a Fabiana para rescisão até isso existir.** |
| 2026-08-03 | **PENDÊNCIA ESTRUTURAL levantada pelo Anderson — controle de contas em aberto.** Hoje cada competência é processada isolada: se uma duplicata é emitida em jan e paga em mar, ou uma compra é parcelada em 10x, **nada lembra que aquela conta precisa ser baixada**. Falta um razão auxiliar por cliente (duplicatas a receber/pagar e parcelas em aberto, com saldo e vencimento) que a Alexandre consulte ao ver um recebimento no extrato e que aponte o que ficou sem baixa no fim do mês. É o próximo item estrutural — sem ele, `1121101` e `2121101` só crescem e a conciliação não fecha. |
| 2026-08-04 | **Razão auxiliar implementado — a memória de contas em aberto (resolve a pendência acima).** `tools/titulos.py`: cada parcela vira um **título** com saldo, vencimento e histórico de baixas, gravado em `data/titulos/{cliente}.json`. As parcelas saem do bloco `<cobr><dup>` da própria NF-e (número, vencimento e valor por parcela — informação que já vinha na nota e ninguém usava); só quando falta é que o vencimento é presumido (+30d), e aí o título fica marcado `presumido: true` para não passar por data confirmada. Regras ganharam `abre_titulo` (venda_a_prazo, servico_a_prazo, compra_mercadoria) e `baixa_titulo` (duplicata_recebida, duplicata_paga_em_atraso, recebimento_duplicata). **REGRA DE OURO: na dúvida não baixa.** `Carteira.candidatos` ordena por (diferença em centavos, distância do vencimento) e `ambiguo()` recusa quando os dois primeiros são indistinguíveis — a pessoa escolhe. Isso apareceu em teste: a tolerância de 2 centavos (que existe para juros) fazia as 3 parcelas de uma nota (301,69 / 301,68 / 301,68) casarem entre si; ordenar por valor exato primeiro corrigiu. Baixar o título errado é pior que não baixar — o saldo total fica certo e a conta do cliente fica errada, erro que não aparece no balancete. `registrar()` é idempotente (reprocessar competência é rotina e não pode duplicar conta a receber). **Testado em real (Alumax, 328 docs):** 322 lançamentos, 0 token, **77 títulos a pagar / R$ 93.197,31**, 48 com vencimento da nota e 29 presumidos; segunda rodada abriu **0 títulos novos** e o saldo não mudou. Novos comandos: `lancamentos -c <cliente>` (o do plano, que faltava) e `titulos -c <cliente> [--vencidos] [--baixar <id> --valor X]`. API do painel: `GET /api/titulos/{cliente}` e `POST /api/titulos/{cliente}/{id}/baixar` (`BaixaIn` no nível do módulo — modelo dentro de `create_app` dá 422). |
| 2026-08-04 | **CFOP classificado no Xavier — decide se a nota gera lançamento** (pedido do Anderson). `tools/cfop.py`: tabela indexada pelos **3 últimos dígitos** (a operação) porque o 1º dígito só diz origem/destino e o sufixo vale igual nos dois sentidos. **Achado ao testar, que mudava tudo: o CFOP é escrito por QUEM EMITIU.** A nota de compra que o cliente recebe vem com `5102` (venda, na ótica do fornecedor) — ler o primeiro dígito faria **toda compra virar venda**. Por isso `classificar(codigo, sentido)` recebe o NOSSO lado (emitente=saída, destinatário=entrada) e ele prevalece. Três desfechos: **gera lançamento** (venda/compra, inclusive ST 5401-5405), **não gera** (remessa, retorno, transferência, industrialização e o `5929`/`1929` — NF-e que só documenta cupom já lançado, que se lançada **dobraria a receita**), e **revisar** (devolução, bonificação, ativo imobilizado, uso e consumo, CFOP genérico `949`, ou fora da tabela). Nota com CFOP de naturezas diferentes não é decidida por maioria: vai para conferência, porque dividir valor entre naturezas exige olhar item a item. Alexandre consulta o CFOP **antes** das regras; o que não contabiliza vai para `notas_sem_lancamento.json` (existe, está certo, e não vira lançamento — para ninguém achar que sumiu). Validado em 268 XMLs reais: 307 venda, 1 bonificação, **0 CFOP fora da tabela**. Efeito em jan/21 da Alumax: a bonificação de R$ 162,30 da Metalúrgica Rocha deixou de virar conta a pagar (61 → 60 títulos). |
| 2026-08-04 | **Competência isolava a pasta errada.** `lancamentos -c X` varria a raiz do cliente, que acumula o que a Raquel baixa do Drive: pedir jan/21 trouxe 6 notas de **junho/2026** (R$ 24.949) para dentro de 2021. Com competência informada, lê só `data/inbox/{cliente}/{AAAA-MM}/`. Números certos de jan/21: 313 docs → 306 lançamentos, 0 token, 60 títulos a pagar / R$ 61.213,59. |
| 2026-08-04 | **À vista × a prazo decidido pelo VENCIMENTO da duplicata** (regra do Anderson, conferida no DANFE que ele mandou). Duplicata com vencimento **diferente** da emissão = a prazo → Fornecedores/Duplicatas a receber, abre título. Vencimento **igual** à emissão, ou nota **sem** duplicata = à vista → credita caixa/banco, não abre título. A simples existência do bloco `<cobr><dup>` não bastava: a NF 44.141 da Estoque do Lojista traz duplicata 001 vencendo 13/01/2021, o mesmo dia da emissão — foi paga na hora e virava conta a pagar eterna. Regras separadas: `compra_a_prazo` (antes de `compra_mercadoria`, que agora credita `@recebimento`). **Efeito em jan/21 da Alumax: 60 → 35 títulos, R$ 61.213,59 → R$ 23.167,61, e zero vencimento presumido** — todo título passou a ter data vinda da nota. Das 42 compras: 16 a prazo, 26 à vista. |
| 2026-08-04 | **Ciclo de ajuste John → Alexandre.** O Anderson pediu: se lançou no caixa e depois aparece a duplicata, a conciliação avisa e alguém ajusta. Implementado nos dois sentidos: (a) o Alexandre, quando um pagamento não acha título, grava o ajuste na carteira (`registrar_ajuste`) em vez de só reportar na rodada — a execução seguinte não teria como redescobrir; (b) o John cruza o `only_bank` da conciliação com os títulos em aberto e, quando um movimento bate **e o título é único**, registra `baixa_nao_lancada`: não é divergência, é baixa que falta lançar. Na dúvida (título ambíguo) o John também não escolhe. Ajustes aparecem em `titulos -c CLIENTE` e em `GET /api/titulos/{cliente}`. **Obs. de nomes:** o Anderson disse "o agente de conciliação informa o Fernando"; o Fernando Batista cuida de certificados digitais, então liguei conciliação (John) → lançamentos (Alexandre), que é quem faz acerto contábil. Confirmar se era essa a intenção. |
| 2026-08-04 | **ERRO GRAVE ACHADO: o DANFE em PDF virava lançamento junto com o XML da mesma nota.** Na Alumax jan/21 eram **43 lançamentos duplicados** (306 → 263) — e passavam despercebidos porque cada um, isolado, parecia correto: data certa, valor certo, contas certas. Só o conjunto estava errado. Descoberto ao investigar por que os pendentes tinham tanto documento que não é lançamento. Agora `NAO_LANCAVEIS` no Alexandre separa, ANTES das regras, o que existe na pasta mas não movimenta conta: **DANFE** ("recebemos de…", "documento auxiliar da nota fiscal"), protocolo de transmissão / Conectividade Social, declaração acessória (SEDIF, GIA, SPED), relatório de notas emitidas, balancete/razão (saída da contabilidade, não entrada) e documento cadastral. Folha e pró-labore ganham motivo próprio: "quem lança é a Fabiana". Vão para `notas_sem_lancamento.json` — existem, estão certos, e ficam listados para ninguém achar que sumiram. **Efeito em jan/21: 7 pendentes → 2**, e os 2 que sobraram são de verdade (um boleto do Banco Inter de R$ 416 sem regra, e a bonificação que precisa de decisão contábil). Conferido no Excel: 211 vendas D caixa/C 4111103, 25 compras à vista D 5131102/C caixa, 16 compras a prazo D 5131102/C 2121101, 4 DAS. |
| 2026-08-04 | **Painel: escolha caixa/banco na competência atrasada.** `FechamentoIn.forma_pagamento` (padrão **caixa**, porque na contabilidade atrasada é o que mais aparece — informação do Anderson) + `<select>` no formulário. Vale para venda à vista e compra paga na hora; compra com duplicata a vencer vai para Fornecedores de qualquer forma. |
| 2026-08-04 | **Painel saiu do Vercel e foi para a VPS** (decisão do Anderson: escritório é home office, quer tudo pelo navegador). Está em **https://lancamentos.escondigital.com.br**, VPS 147.79.86.221 (`srv653140`), no lugar do sistema antigo de lançamentos (`/docker/lancamentos`, que guardava dados no Supabase — nada foi perdido; container parado, imagem/pasta/backup intactos para rollback). **Motivo de fundo:** no Vercel só havia conversa — os agentes são Python e precisam ler XML, PDF, OneDrive e Radar; o chat respondia *como se* tivesse lançado. Por isso as personas do `api/chat.js` ganharam regra absoluta: não inventar execução, mandar para o painel. Detalhes em `docs/VPS.md`. Autenticação por `basicauth` do Traefik (o painel FastAPI não tem login e ia expor 108 clientes na internet) — senha no Caddy/Traefik é uma linha e não depende de acertar sessão/cookie/CSRF em Python. **Duas armadilhas caras:** (1) gerar o hash dentro de `ssh "..."` faz o shell REMOTO expandir `$2y`/`$05` — o hash chegava corrompido; gerar no servidor com heredoc `<<'REMOTO'`; (2) **o Traefik ficou com configuração velha em cache e devolvia 401 mesmo sem middleware algum**, sem log nenhum, enquanto o container respondia 200 no IP direto — `docker restart traefik-qsuz-traefik-1` resolveu. **Nesta VPS também rodam EsconZap (Secretaria), atendemei e financeiro**: nunca `docker compose down` fora de `/opt/escon-agentes`. Confirmado no ar: 110 clientes, 16 agentes, títulos da Alumax (R$ 23.167,61). Sem chave de LLM no servidor — não afeta Alexandre/Fabiana (rodam por regra), afeta o chat com o Max. |
| 2026-08-04 | **VPS operacional: dois logins Microsoft + LLM + chat do painel corrigido.** (1) `graph-login --perfil mail|arquivos` criado (faltava; o `deploy_vps.sh` já o citava): mostra qual conta espera antes do device code, porque errar a conta aqui foi o erro mais caro do projeto. Autorizados e conferidos pelo UPN dentro do token: `mail` = contato@, `arquivos` = anderson@. **Lição operacional:** recriar o container durante um device flow pendente **mata o processo antes de gravar o token** — foi o que aconteceu ao aplicar a chave do OpenRouter, e o Anderson teve de refazer. Não mexer no container enquanto há login em curso. Os tokens sobrevivem a rebuild porque ficam no volume `agentes_msal`. (2) Chave do OpenRouter no `.env` do servidor com merge que preserva as chaves só-do-servidor (`PAINEL_BASICAUTH`, `DASHBOARD_HOST`): painel saiu de `offline` para `openrouter/deepseek-v4-flash`. (3) **`/api/run` devolvia 422** — `QuickRun` estava definido DENTRO de `create_app`, o mesmo bug do `FechamentoIn` que já tinha acontecido: com `from __future__ import annotations` o Pydantic não resolve os tipos de classe aninhada. Movido para o nível do módulo. Chat do painel testado no ar: roteia para o Alexandre e ele responde corretamente "Nenhum documento em /app/data/inbox". |
| 2026-08-04 | **Painel de revisão: lançamentos em tela, pendente vira regra conversando, planilha só no fim.** Pedido do Anderson depois do primeiro fechamento real. (1) **Revisão em três abas** — Lançados / Aguardando você / Sem lançamento — dentro do card da competência; a planilha deixou de ser o único jeito de ver o resultado (conferir dentro de um Excel baixado é onde o erro passa). Clicar numa competência já processada reabre a revisão: atraso é de meses, ninguém resolve tudo numa sentada. (2) **`tools/aprendizado.py` + `config/regras_aprendidas.yaml`**: o pendente é clicado, o painel mostra o texto do documento e **sugere o que o identifica**, a pessoa escolhe as contas numa lista (nunca digita código — foi digitando de cabeça que o plano de contas foi parar em IPI) e a regra nasce. Avaliadas ANTES das genéricas: correção humana sobre caso concreto vence palpite geral. Depois de gravar, **reprocessa a competência** sem rebaixar os documentos do OneDrive. **Caso real que motivou tudo:** o boleto de R$ 528 da Alumax não diz "honorário" em lugar nenhum — diz o beneficiário `09.377.184/0001-88 DIAS DE PAULA ESCRITORIO DE APOIO`. Nenhuma regra genérica pegaria; nenhum modelo adivinharia com segurança. Ensinado uma vez: set/2024 foi de 50 → 51 lançamentos e 3 → 2 pendentes. (3) **`tools/recorrentes.py`**: despesa de contrato provisionada todo mês **com ou sem documento** — em vários meses atrasados o boleto simplesmente não está na pasta, e sem isso a despesa some e o resultado sai errado para mais. Entra no último dia do mês, marcada `origem: recorrente` para separar o que veio de documento do que veio de contrato. Validado: out/2024 (284 docs → 265 lançamentos) trouxe a provisão de R$ 528 sozinho, sem nada ter sido ensinado para outubro. (4) Agentes viraram clicáveis: mostra o que cada um executou. Detector de design da skill impeccable: 0 achados. |
